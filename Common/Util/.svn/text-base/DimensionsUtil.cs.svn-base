using System;
using System.Collections.Generic;
using System.Text;

namespace VideoPaintballCommon.Util
{
    public static class DimensionsUtil
    {
        public static float GetMapWidth()
        {
            return 120 * CalculateOffset();
        }

        public static float GetMapHeight()
        {
            return 90 * CalculateOffset();
        }

        public static float GetAvatarWidth()
        {
            
            return 8 * CalculateOffset();
        }

        public static float GetAvatarHeight()
        {
            return 8 * CalculateOffset();
        }

        public static float GetAvatarShieldOffset()
        {
            return 3 * CalculateOffset();
        }

        public static float GetGunWidth()
        {
            return 2 * CalculateOffset();
        }

        public static float GetGunHeight()
        {
            return 2 * CalculateOffset();
        }

        public static float GetPaintballWidth()
        {
            return 3 * CalculateOffset();
        }

        public static float GetPaintballHeight()
        {
            return 3 * CalculateOffset();
        }

        public static float GetHealthMeterWidth()
        {
            return 1 * CalculateOffset();
        }

        public static float GetHealthMeterHeight()
        {
            return 4 * CalculateOffset();
        }

        public static float GetAmmoMeterWidth()
        {
            return 1 * CalculateOffset();
        }

        public static float GetAmmoMeterHeight()
        {
            return 4 * CalculateOffset();
        }

        public static float GetObstacleWidth(float width)
        {
            return width; //* CalculateOffset();
        }

        public static float GetObstacleHeight(float height)
        {
            return height; //* CalculateOffset();
        }

        public static float GetPaintballOffset()
        {
            return GetAvatarShieldOffset() * 2;
        }

        private static float CalculateOffset()
        {
            //TODO: figure out how to do this the right way
            return 7;
        }
    }
}
